/**
 * Module dependencies
 */

@require './variables'

/**
 * @type mixin
 * @type opaque
 */

clearfix()
  if clearfix--support-ie
    *zoom 1
  &:before
  &:after
    content " "
    display table
  &:after
    clear both
