/**
 * Module dependencies
 */

@require './variables'

/**
 * Define a generic mixin
 */

clearfix()
  if clearfix--support-ie
    *zoom 1
  &:before
  &:after
    content " "
    display table
  &:after
    clear both
